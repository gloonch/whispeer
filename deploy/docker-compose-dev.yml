name: whispeer

services:
  client:
    image: node:20-alpine
    working_dir: /app
    env_file: .dev.env
    ports:
      - "5173:5173"
    volumes:
      - ../client:/app
      - client_node_modules:/app/node_modules
    command: sh ./scripts/dev-entrypoint.sh

volumes:
  client_node_modules:
