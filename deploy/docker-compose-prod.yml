name: whispeer

services:
  client:
    build:
      context: ../client
      dockerfile: Dockerfile
      target: prod
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL}
        VITE_IMAGE_BASE_URL: ${VITE_IMAGE_BASE_URL}
    env_file: .prod.env
    ports:
      - "3000:80"
